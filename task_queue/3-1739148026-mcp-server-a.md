# Task: MCPサーバーA (WebシステムA向け)

role: core-dev
priority: 3
created_at: 2026-02-10T01:00:00Z

## Description

Create mcp-servers/server_a/ - MCP server for Web System A using Selenium WebDriver (headless Chrome). Implement Page Object pattern in page_objects/ directory: LoginPage (navigate to login URL, enter credentials, submit, verify login success), OrderFormPage (navigate to order form, fill all fields from OrderInputData, handle dropdowns/date pickers), ConfirmationPage (verify submitted data matches input, capture confirmation number). server.py: MCP server that accepts execute_input tool calls, runs the Selenium workflow, captures screenshots at each step, returns ExecutionResult. Include retry logic (max 3 attempts) with exponential backoff. Credentials loaded from DB (WebSystemCredential). Dockerfile for this server. Write tests with mocked Selenium.
