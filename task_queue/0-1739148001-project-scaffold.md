# Task: プロジェクト骨格の構築

role: core-dev
priority: 0
created_at: 2026-02-10T00:45:00Z

## Description

Create docker-compose.yml (backend FastAPI + frontend React + PostgreSQL 16), backend scaffold (FastAPI app with main.py, config.py, database.py, requirements.txt, Dockerfile), frontend scaffold (React 18 + TypeScript + Vite + TailwindCSS + React Router v7, package.json, Dockerfile). Ensure `docker compose up` boots all 3 services. Follow the directory structure defined in CLAUDE.md.
